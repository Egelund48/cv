image: node:20

pages:
  stage: deploy
  script:
    - npm install
    - npm run build
    - mv dist public   # GitLab Pages serves the `public` folder
  artifacts:
    paths:
      - public
  only:
    - main   # deploy only from the main branch
